import styled from 'styled-components/native';
// Assets
import { Calendar } from '../../assets';

export const Container = styled.TouchableOpacity``;

export const CalendarIcon = styled(Calendar).attrs({ width: 25, height: 25 })``;
